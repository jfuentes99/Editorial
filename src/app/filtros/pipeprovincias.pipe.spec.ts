import { PipeprovinciasPipe } from './pipeprovincias.pipe';

describe('PipeprovinciasPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeprovinciasPipe();
    expect(pipe).toBeTruthy();
  });
});

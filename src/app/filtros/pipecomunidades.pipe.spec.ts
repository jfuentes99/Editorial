import { PipecomunidadesPipe } from './pipecomunidades.pipe';

describe('PipecomunidadesPipe', () => {
  it('create an instance', () => {
    const pipe = new PipecomunidadesPipe();
    expect(pipe).toBeTruthy();
  });
});
